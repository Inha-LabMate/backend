# 환경 변수 예시
# 실제 사용 시 .env 파일로 복사하고 값을 수정하세요

# 데이터베이스 설정 (PostgreSQL 모드)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=labsearch
DB_USER=postgres
DB_PASSWORD=your_password_here

# 임베딩 설정
EMBEDDING_MODEL=multilingual-e5-large
EMBEDDING_DEVICE=cpu  # cpu 또는 cuda

# 저장소 모드
USE_LOCAL=true  # true: 로컬 JSON, false: PostgreSQL

# 데이터 경로
DATA_DIR=./data/crawl_data
CACHE_DIR=./data/crawl_cache

# API 설정 (선택)
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=true

# 로그 레벨
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR
