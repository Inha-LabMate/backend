# ============================================================================
# 인하대 연구실 검색 시스템 - 의존성 패키지
# ============================================================================
# Python 3.8+ 필요
# 설치 방법: pip install -r requirements.txt
# Playwright 추가 설치: python -m playwright install chromium

# ============================================================================
# 웹 크롤링 (JavaScript 렌더링 지원)
# ============================================================================
playwright>=1.40.0       # ⭐ 핵심: JavaScript 실행 가능 (Google Sites, Wix 등)
beautifulsoup4>=4.12.0   # HTML 파싱
lxml>=4.9.0              # XML/HTML 파서 (빠름)

# ============================================================================
# 데이터 처리
# ============================================================================
pandas>=2.0.0            # 데이터프레임 처리
numpy>=1.24.0            # 수치 연산

# ============================================================================
# AI/ML - 임베딩 & 검색
# ============================================================================
sentence-transformers>=2.2.0  # 텍스트 임베딩 (multilingual-mpnet)
torch>=2.0.0                  # PyTorch (sentence-transformers 의존성)

# ============================================================================
# 데이터베이스
# ============================================================================
psycopg2-binary>=2.9.0   # PostgreSQL 연결 (선택 - 로컬 JSON 모드도 지원)
# 참고: pgvector는 PostgreSQL 서버에 별도 설치 필요

# ============================================================================
# PDF 처리 (선택 - 연구실 자료 크롤링 시)
# ============================================================================
pdfminer.six>=20221105   # PDF 텍스트 추출

# ============================================================================
# API 서버
# ============================================================================
fastapi>=0.104.0         # REST API 프레임워크
uvicorn>=0.24.0          # ASGI 서버

# ============================================================================
# 유틸리티
# ============================================================================
python-dotenv>=1.0.0     # 환경 변수 관리 (.env 파일)
tqdm>=4.65.0             # 진행률 표시

# ============================================================================
# 개발 도구
# ============================================================================
pytest>=7.4.0            # 테스트 프레임워크
black>=23.0.0            # 코드 포맷터
flake8>=6.0.0            # 린터 (코드 스타일 검사)

# 1단계: 후보군 생성에 필요한 패키지들

# 키워드 검색 (BM25)
rank-bm25>=0.2.2

# 의미 검색 (E5-small 임베딩)
sentence-transformers>=2.2.2

# 기본 패키지
numpy>=1.24.0
torch>=2.0.0

# 선택적: 빠른 벡터 검색을 위한 패키지
# faiss-cpu>=1.7.4  # 대용량 벡터 DB 사용 시